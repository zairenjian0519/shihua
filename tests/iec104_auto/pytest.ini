[pytest]
testpaths = .
addopts = -ra
markers =
    link: IEC104 link layer tests.
    standard: Standard IEC104 service tests.
    custom: Oilfield custom TypeID tests.
    active: Active upload tests that use the diagnostic port.
    negative: Negative and error handling tests.
    slow: Tests that wait for protocol timers or delayed uploads.
