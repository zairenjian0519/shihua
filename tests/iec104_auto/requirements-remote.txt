# Optional. Install this only when using --manage-slave / --capture-pcap.
#
# The current test host uses Python 3.7 and pip 19.x. Newer paramiko/bcrypt
# releases can pull pyproject builds that old pip cannot parse or build on
# Windows, so keep these versions conservative and wheel-friendly.
paramiko==2.12.0
bcrypt==3.2.2
cryptography==38.0.4
PyNaCl==1.5.0
cffi==1.15.1
